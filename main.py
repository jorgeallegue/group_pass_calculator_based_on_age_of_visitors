# Serves as the entry point of the application, starting the application by calling the controller's run_app function.
from controllers.controller import run_app 

if __name__ == "__main__": # Imports and calls the run_app function from controller.py, starting the application.
    run_app()